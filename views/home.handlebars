<main class="mg-cus-nav">
    <h1 class="welcome text-center">Welcome, {{firstName}}!</h1>

    <section class="feed pe-auto">
        {{#each recipes as |recipe|}}
            <div class="post-card feed green-box m-5" id="{{recipe.id}}">
                <h2>{{recipe.name}}</h2>
                <p class="description">{{recipe.description}}</p>
                <p class="author"><a href="../profile/{{recipe.user.id}}">{{recipe.user.first_name}} {{recipe.user.last_name}}</a> </p>
                {{#each recipe.ingredients as |ingredient|}}
                    <span class="ingredient" data-value="{{ingredient.name}}"></span>
                {{/each}}
            </div>
        {{/each}}
    </section>
</main>


{{!-- <script src="/js/home.js"></script> --}}
<script src="/js/eventListeners.js"></script>